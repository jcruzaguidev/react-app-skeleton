import styled from 'styled-components';

export const StylesSuccess = styled.div`
   .success-box {
      padding-top: 100px;
      text-align:center;
   }
   .title {
      line-height: 1.75rem;
      text-align: center;
      font-weight: 700;
   }
   .message {
      font-size: 14px;
   }
   .loader-message {
      font-size: 12px;
   }
   .header-image {
      max-width: 150px;
   }
   .loader {
      width: 30px;
   }
`;
